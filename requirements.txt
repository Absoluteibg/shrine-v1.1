# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20   # required for form parsing (admin forms, file uploads)
itsdangerous==2.2.0        # required by Starlette SessionMiddleware (signs the session cookie)

# --- Auth ---
bcrypt==4.2.1               # password hashing for the single admin account

# --- Templating ---
jinja2==3.1.5

# --- Database / ORM / migrations ---
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.12     # PostgreSQL driver — production database (see DATABASE_URL in .env)

# --- Config & validation ---
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Testing ---
pytest==8.3.4
httpx==0.28.1               # required by fastapi.testclient.TestClient
