// SHRINE — main client-side entrypoint.
// Empty on purpose: no JS-driven behavior exists yet. Kept as a single
// entrypoint so future interactive features (admin forms, reading
// progress, etc.) have one obvious place to live rather than scattered
// inline <script> tags.
